{
  "name": "Thunder CMS heroku app",
  "buildpacks": [
    {
      "url": "https://github.com/thunder/heroku-buildpack-thunder#use-postgres"
    }
  ],
  "environments": {
    "review": {
      "addons": ["heroku-postgresql:hobby-dev"]
    }
  },
  "scripts": {
    "postdeploy": "vendor/bin/drush si thunder --account-name=admin --account-pass=admin --account-mail=admin@example.com --site-mail=admin@example.com --site-name=$HEROKU_APP_NAME --locale=en -y; vendor/bin/drush -y en thunder_demo"
  }
}
